language: python
cache: pip
python:
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install -U numpy
  - pip install .[dev]

# run tests
script:
  - cd tests
  - py.test

jobs:
  include:
    - stage: compliance
      script: pre-commit run --all-files
stages:
  - compliance
  - test
